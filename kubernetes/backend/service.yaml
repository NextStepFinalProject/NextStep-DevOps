apiVersion: v1
kind: Service
metadata:
  name: nextstep-backend-external-app
spec:
  ports:
  - name: nextstep-backend-port
    port: 3000
    protocol: TCP
    targetPort: 3000
  clusterIP: None
  type: ClusterIP
